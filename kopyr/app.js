const P=window.KP||[];let active='Todos',cart=JSON.parse(localStorage.getItem('kopyr-cart')||'[]');const $=x=>document.getElementById(x),money=n=>'RD$'+Number(n||0).toLocaleString('es-DO');function img(p){return p.driveId?'https://lh3.googleusercontent.com/d/'+p.driveId+'=w900':''}function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function card(p){return `<article class="card"><div class="photo"><img loading="lazy" src="${img(p)}" alt="${esc(p.name)}" onerror="this.style.display='none';this.nextElementSibling.style.display='flex'"><div class="fallback">${esc(p.brand)}<br>${esc(p.name)}</div><span class="badge">PIEZA ÚNICA</span></div><div class="cardbody"><div class="meta">${esc(p.sub||p.category)}</div><div class="name">${esc(p.name)}</div><div class="brand">${esc(p.brand)}</div><div class="row"><div><div class="price">${money(p.price)}</div><div class="size">${p.size?'Talla '+esc(p.size):'Consulta talla'}</div></div><button class="add" onclick="add('${p.sku}')">+</button></div></div></article>`}function render(){let q=$('q').value.toLowerCase().trim(),a=P.filter(p=>(!q||JSON.stringify(p).toLowerCase().includes(q))&&(active==='Todos'||p.category===active||(active==='150'&&p.price<=150)));let s=$('sort').value;if(s==='low')a.sort((a,b)=>a.price-b.price);if(s==='high')a.sort((a,b)=>b.price-a.price);if(s==='az')a.sort((a,b)=>a.name.localeCompare(b.name,'es'));$('count').textContent=a.length;$('grid').innerHTML=a.map(card).join('')}function setF(f){active=f;document.querySelectorAll('.fbtn').forEach(b=>b.classList.toggle('on',b.dataset.f===f));render()}function filterCat(f){setF(f);goShop()}function goShop(){document.getElementById('tienda').scrollIntoView({behavior:'smooth'})}function add(s){if(!cart.includes(s))cart.push(s);localStorage.setItem('kopyr-cart',JSON.stringify(cart));draw();toast('Agregado a tu selección')}function remove(s){cart=cart.filter(x=>x!==s);localStorage.setItem('kopyr-cart',JSON.stringify(cart));draw()}function draw(){let a=cart.map(s=>P.find(p=>p.sku===s)).filter(Boolean);$('cartCount').textContent=a.length;$('total').textContent=money(a.reduce((n,p)=>n+p.price,0));$('ditems').innerHTML=a.length?a.map(p=>`<div class="ditem"><strong>${esc(p.name)}</strong><small>${esc(p.brand)} · ${money(p.price)}${p.size?' · Talla '+esc(p.size):''}</small><br><button class="remove" onclick="remove('${p.sku}')">Quitar</button></div>`).join(''):'<p style="color:#777">Aún no has agregado piezas.</p>'}function openCart(){$('drawer').classList.add('on');$('overlay').classList.add('on')}function closeCart(){$('drawer').classList.remove('on');$('overlay').classList.remove('on')}function sendWA(){let a=cart.map(s=>P.find(p=>p.sku===s)).filter(Boolean);if(!a.length)return toast('Agrega al menos una pieza');let t=['Hola KOPYR STORE 👋','Quiero reservar estas piezas:','',...a.map(p=>'• '+p.name+' — '+money(p.price)+(p.size?' — Talla '+p.size:'')+' ['+p.sku+']'),'','Total estimado: '+money(a.reduce((n,p)=>n+p.price,0)),'','Por favor confírmenme disponibilidad antes de pagar.'].join('\n');window.open('https://wa.me/18296480491?text='+encodeURIComponent(t),'_blank')}let to;function toast(t){clearTimeout(to);$('toast').textContent=t;$('toast').classList.add('on');to=setTimeout(()=>$('toast').classList.remove('on'),1700)}$('q').oninput=render;$('sort').onchange=render;document.querySelectorAll('.fbtn').forEach(b=>b.onclick=()=>setF(b.dataset.f));render();draw();