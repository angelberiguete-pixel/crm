"use client";
import {FormEvent,useEffect,useState} from "react";
import Link from "next/link";
import {useRouter} from "next/navigation";
import {supabase} from "@/lib/supabase";
export default function ResetPassword(){const router=useRouter();const[ready,setReady]=useState(false),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState("");useEffect(()=>{let alive=true;const{data}=supabase.auth.onAuthStateChange((event,session)=>{if(alive&&session&&(event==="PASSWORD_RECOVERY"||event==="INITIAL_SESSION"))setReady(true)});supabase.auth.getSession().then(({data,error})=>{if(alive){if(error)setMessage(error.message);else setReady(!!data.session)}});return()=>{alive=false;data.subscription.unsubscribe()}},[]);async function save(e:FormEvent){e.preventDefault();if(busy)return;if(password!==confirm)return setMessage("Las contraseñas no coinciden.");setBusy(true);try{const r=await supabase.auth.updateUser({password});if(r.error)throw r.error;router.replace("/revenue-command-center")}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}return <main className="auth-shell"><section className="auth-panel"><div className="eyebrow">AUREVECTOR</div><h1>Nueva contraseña</h1>{ready?<form className="stack gap-16" onSubmit={save}><label>Contraseña<input type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirmar contraseña<input type="password" autoComplete="new-password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button disabled={busy} className="button primary">{busy?"Guardando…":"Guardar contraseña"}</button></form>:<p>Abre el enlace de recuperación que recibiste por correo. Si ha caducado, solicita uno nuevo.</p>}{message&&<p className="notice" role="status">{message}</p>}<Link href="/login" className="text-button">Volver al acceso</Link></section></main>}
